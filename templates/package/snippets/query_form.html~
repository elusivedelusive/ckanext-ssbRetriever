{% import 'macros/form.html' as form %}

  {% set prefix = h.url_for(controller='package', action='read', id='') %}
  {% set domain = h.url_for(controller='package', action='read', id='', qualified=true) %}
  {% set domain = domain|replace("http://", "")|replace("https://", "") %}
  {% set attrs = {'data-module': 'slug-preview-slug', 'data-module-prefix': domain, 'data-module-placeholder': '<dataset>'} %}

{% block query_form %}
  {{ form.input('ssb-query', label=_('ssb-query'), id='field-ssb-query', placeholder=_('Enter Query Here'), value=data.ssb-query, error=errors.ssb-query, classes=['control-medium']) }}

  {{ form.input('ssb-url', label=_('ssb-url'), id='field-ssb-url', placeholder=_('Enter SSB Table URL here'), value=data.ssb-url, error=errors.ssb-url, classes=['control-medium']) }}

{% endblock %}
