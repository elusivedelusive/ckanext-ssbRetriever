{% import 'macros/form.html' as form %}

<form id="resource-edit" class="dataset-form dataset-resource-form form-horizontal" method="post" action="{{ action }}" data-module="basic-form resource-form" enctype="multipart/form-data">

<input name="id" value="{{ data.id }}" type="hidden"/>

	{% block query_form %}
  	{{ form.input('ssb_url', label=_('SSB URL'), id='field_ssb_url', placeholder=_('Enter SSB Table URL Here'), value=data.ssb_url, 		error=errors.ssb_url, classes=['control-medium'])}}

  	{{ form.input('ssb_query', label=_('SSB Query'), id='field_ssb_query', placeholder=_('Enter Query Here'), type=url, value=data.ssb_query, error=errors.ssb_query, classes=['control-medium']) }} 
	{% endblock %}

  <div class="form-actions">
      {% block add_button %}
	<ul>
		{% for group in h.ssbRetriever_test() %}
			<li< {{group.display_name}} </li>
		{% endfor %}
	</ul>
      <button class="btn btn-primary" name="save" value="go-dataset-complete" action="runQuery" type="submit" >{{ _('Add') }}</button>
      {% endblock %}
  </div>
</form>
